# AGENTS.md

## 项目简介

本项目是 Sesame Robot 的二次开发版本，目标是开发一台具有视觉、语音、智能跟随、自动避障和大模型交互能力的四足桌面机器人。

项目必须分阶段开发，不要一次加入多个大型功能。

## 项目目录

- `firmware/`：ESP32 固件、舵机、动作、OLED、Wi-Fi 和控制接口
- `hardware/`：CAD、PCB、电路、接线和 3D 打印文件
- `software/sesame-studio/`：动作编辑软件
- `scripts/`：编译和检查脚本
- `ai-controller/`：视觉、语音和大模型程序
- `development-notes/`：开发记录和架构文档

## 系统架构

项目采用双控制器架构。

### ESP32 负责

- 8 个舵机实时控制
- 四足步态执行
- OLED 表情
- IMU 和距离传感器
- 急停和通信超时保护
- 电池监测
- 本地硬件安全检查

### AI 主控制器负责

- 摄像头读取
- 人体和物体检测
- 人脸识别
- 主人追踪
- 语音识别和语音合成
- 大语言模型对话
- 高层行为决策
- 记忆和情绪状态估计

禁止把大型视觉模型或完整大语言模型加入 ESP32 固件。

## 已验证开发环境

- Arduino CLI：1.5.1
- ESP32 Arduino Core：3.3.10
- ESP32Servo：3.0.9
- Adafruit SSD1306：2.5.17
- Adafruit GFX Library：1.12.6
- 默认开发板：`esp32:esp32:lolin_s2_mini`

除非任务明确要求，否则不要升级依赖。

`ESP32Servo` 必须保持为 `3.0.9`，升级前必须进行兼容性测试。

## 固件验证

修改 `firmware/` 后，必须执行：

powershell -ExecutionPolicy Bypass -File ".\scripts\firmware-build.ps1"

只有编译成功后，固件任务才算完成。

任务总结必须说明：

- 编译是否成功
- 程序存储空间占用
- 动态内存占用
- 重要警告
- 尚未进行的真实硬件测试

## 硬件安全规则

未经用户明确确认，禁止：

- 烧录真实开发板
- 执行 `arduino-cli upload`
- 打开或控制真实串口
- 驱动真实舵机
- 控制机器人行走
- 禁用急停、避障和通信超时保护
- 取消舵机角度限制

AI 主控制器只能发送高级动作请求，ESP32 本地安全层拥有最终决定权。

## 固件修改规则

- 保留原有动作，除非任务明确要求修改
- 不要随意改变舵机引脚
- 不要硬编码 Wi-Fi 密码、API Key 或 Token
- 不要提交 `.build/`
- 优先进行小范围、可验证的修改
- 硬件控制和高层行为必须分离
- 新增命令或 API 字段时必须补充文档
- 通信失联时机器人必须自动停止

## AI 控制器规则

AI 代码统一放在 `ai-controller/`。

AI 控制器应发送高级命令，例如：

- `stand`
- `rest`
- `walk_forward`
- `turn_left`
- `turn_right`
- `stop`
- `wave`
- `follow_owner`
- `stop_following`
- `set_face`
- `emergency_stop`

AI 控制器不应持续直接控制每个舵机角度。

## 隐私规则

- 禁止提交 API Key、Token、Wi-Fi 密码等敏感数据
- 人脸照片、特征数据和录音默认只保存在本地
- 未经用户同意，不得上传私人媒体到第三方服务

## Git 工作规则

当前主要开发分支：

`feat/ai-robot-v0`

完成任务前必须：

1. 查看 `git diff`
2. 运行对应验证
3. 汇总修改文件
4. 说明未测试的硬件行为
5. 未经用户要求，不要自行提交或推送

## 当前开发顺序

1. 保证原版固件稳定编译
2. 建立统一机器人控制接口
3. 建立电脑端摄像头程序
4. 实现人体和物体检测
5. 实现主人人脸识别和追踪
6. 实现安全跟随和自动避障
7. 接入语音和大模型助手
8. 将 AI 系统迁移到板载计算机
9. 实现跌倒检测、自动起身和地面自适应
10. 实现自动返回充电底座